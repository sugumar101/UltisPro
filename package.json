{
  "name": "ultispro",
  "private": true,
  "version": "0.1.0",
  "description": "UltisPro - multi-tenant retail billing & inventory management SaaS",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web\"",
    "build": "npm run build --workspace=@ultispro/shared-types && npm run build --workspaces --if-present",
    "lint": "eslint . && npm run lint --workspace=apps/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "format": "prettier --write .",
    "migrate": "npm run migrate --workspace=apps/api",
    "postinstall": "npm run build --workspace=@ultispro/shared-types"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "eslint": "^9.13.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.11.0"
  }
}
